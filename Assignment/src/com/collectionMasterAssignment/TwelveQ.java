package com.collectionMasterAssignment;

public class TwelveQ
{
	/*
	 12. State True or False
1. Array List maintains insertion order.
True - ArrayList maintains insertion order.

2. HashMap implements collection interface.
False - HashMap does not directly implement the Collection interface.

3. LinkedHashMap is synchronized.
False - LinkedHashMap is not synchronized by default.
 If synchronization is needed, you can use Collections.synchronizedMap(new LinkedHashMap<>()).

4. HashMap maintains insertion order.
False - HashMap does not maintain insertion order.
 It may seem ordered due to the internal implementation, but it is not guaranteed.

5. LinkedHashMap allows null values.
True - LinkedHashMap allows null values.

6. ConcurrentHashMap locks entire map.
False - ConcurrentHashMap does not lock the entire map. 
It allows concurrent reads and updates to different segments.

7. TreeMap does not allow null values.
True - TreeMap does not allow null values.

8. HashSet allows duplicate values.
False - HashSet does not allow duplicate values.


9. ArrayList allows multiple null values.
True - ArrayList allows multiple null values.

10. LinkedHashMap allows null key.
True - LinkedHashMap allows a null key.

11. Duplicate values are not allowed in HashMap.
False - Duplicate values are allowed in HashMap, but they cannot have duplicate keys.

12. LinkedHashMap stores entries in ascending order.
False - LinkedHashMap maintains insertion order, not necessarily in ascending order.

13. Insertion order is maintained in TreeMap.
True - Insertion order is maintained in TreeMap.

14. Treemap performs better than HashMap and LinkedHashMap
False - The performance characteristics of TreeMap, HashMap,
 and LinkedHashMap depend on the specific use case. There is no general rule that TreeMap performs better.

15. Vector maintains insertion order.
True - Vector maintains insertion order.

	 */
}
